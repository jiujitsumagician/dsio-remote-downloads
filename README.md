# DSIO Remote — downloads

Public download host for the **DSIO Remote Hub** installer. The source lives in a private repo;
only the release binaries are here so the DSUI download link works without a GitHub login.

The Hub does nothing without the operator's own Cloudflare authorization, so the binary is inert
on its own. See the Releases tab for the latest Hub download.
